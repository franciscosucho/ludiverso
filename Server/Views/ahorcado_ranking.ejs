<!DOCTYPE html>
<html lang="es">

<head>
    <%- include('head.ejs') %>
    <title>Ranking Ahorcado</title>
    <style>
        .ranking-container {
            max-width: 1200px;
            margin: 120px auto 2rem;
            padding: 2rem;
            background: var(--Fondo_principal);
            border-radius: 15px;
            box-shadow: 0 4px 20px var(--box_shadow);
            border: 1px solid var(--back_card);
            transition: all 0.3s ease;
        }

        .ranking-header {
            text-align: center;
            margin-bottom: 2rem;
            color: var(--Texto_principal);
        }

        .ranking-header h1 {
            font-size: 2.5rem;
            margin-bottom: 0.5rem;
            background: linear-gradient(135deg, var(--Encabezados_botones_y_primarios), var(--Botones_secundarios_o_hover_azul));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .ranking-header p {
            color: var(--Texto_secundario);
            font-size: 1.1rem;
        }

        .ranking-filters {
            display: flex;
            gap: 1rem;
            margin-bottom: 2rem;
            justify-content: center;
            flex-wrap: wrap;
        }

        .filter-button {
            padding: 0.75rem 1.5rem;
            border: 2px solid var(--Encabezados_botones_y_primarios);
            border-radius: 25px;
            background: transparent;
            color: var(--Encabezados_botones_y_primarios);
            cursor: pointer;
            transition: all 0.3s ease;
            font-weight: 600;
            font-size: 0.95rem;
        }

        .filter-button:hover {
            background: var(--Encabezados_botones_y_primarios);
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(62, 134, 211, 0.3);
        }

        .filter-button.active {
            background: var(--Encabezados_botones_y_primarios);
            color: white;
            box-shadow: 0 4px 12px rgba(62, 134, 211, 0.3);
        }

        .stats-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }

        .stat-card {
            background: var(--back_card);
            padding: 1.5rem;
            border-radius: 15px;
            text-align: center;
            border: 1px solid var(--box_shadow);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .stat-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, var(--Encabezados_botones_y_primarios), var(--Botones_secundarios_o_hover_azul));
        }

        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px var(--box_shadow);
        }

        .stat-card h3 {
            color: var(--Texto_principal);
            margin: 0 0 0.5rem 0;
            font-size: 1.1rem;
            font-weight: 600;
        }

        .stat-card p {
            color: var(--Encabezados_botones_y_primarios);
            font-size: 2rem;
            font-weight: bold;
            margin: 0;
        }

        .ranking-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 1rem;
            background: var(--Fondo_principal);
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 4px 20px var(--box_shadow);
        }

        .ranking-table th {
            background: var(--Encabezados_botones_y_primarios);
            color: white;
            padding: 1.2rem 1rem;
            text-align: center;
            font-weight: 600;
            font-size: 0.95rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .ranking-table td {
            padding: 1rem;
            text-align: center;
            border-bottom: 1px solid var(--back_card);
            color: var(--Texto_principal);
            transition: all 0.3s ease;
        }

        .ranking-row {
            transition: all 0.3s ease;
        }

        .ranking-row:hover {
            background: var(--hover_card);
            transform: scale(1.01);
        }

        .ranking-row.victoria {
            background: rgba(40, 167, 69, 0.1);
            border-left: 4px solid #28a745;
        }

        .ranking-row.derrota {
            background: rgba(220, 53, 69, 0.1);
            border-left: 4px solid #dc3545;
        }

        .medal {
            font-size: 1.5rem;
            margin-right: 0.5rem;
            filter: drop-shadow(0 2px 4px rgba(0,0,0,0.2));
        }

        .back-button {
            display: inline-block;
            margin: 1rem;
            padding: 1rem 2rem;
            background: linear-gradient(135deg, var(--Encabezados_botones_y_primarios), var(--Botones_secundarios_o_hover_azul));
            color: white;
            text-decoration: none;
            border-radius: 25px;
            transition: all 0.3s ease;
            font-weight: 600;
            box-shadow: 0 4px 15px rgba(62, 134, 211, 0.3);
        }

        .back-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 20px rgba(62, 134, 211, 0.4);
        }

        .area-title {
            color: var(--Texto_principal);
            margin: 2rem 0 1rem;
            text-align: center;
            font-size: 1.8rem;
            font-weight: 600;
            background: linear-gradient(135deg, var(--Encabezados_botones_y_primarios), var(--Botones_secundarios_o_hover_azul));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        /* Responsive design */
        @media (max-width: 768px) {
            .ranking-container {
                margin: 100px 1rem 2rem;
                padding: 1rem;
            }

            .ranking-header h1 {
                font-size: 2rem;
            }

            .stats-container {
                grid-template-columns: 1fr;
            }

            .ranking-table {
                font-size: 0.9rem;
            }

            .ranking-table th,
            .ranking-table td {
                padding: 0.75rem 0.5rem;
            }

            .filter-button {
                padding: 0.6rem 1.2rem;
                font-size: 0.9rem;
            }
        }

        @media (max-width: 480px) {
            .ranking-table {
                font-size: 0.8rem;
            }

            .ranking-table th,
            .ranking-table td {
                padding: 0.5rem 0.25rem;
            }

            .medal {
                font-size: 1.2rem;
            }
        }
    </style>
</head>

<body>
    <%- include('header.ejs') %>
    <main id="cont_main_areas">
        <div class="ranking-container">
            <div class="ranking-header">
                <h1>🏆 Ranking del Ahorcado</h1>
                <p>¡Compite por el primer lugar en cada área!</p>
            </div>

            <div class="ranking-filters">
                <button class="filter-button <%= !area ? 'active' : '' %>" onclick="window.location.href='/ahorcado_ranking'">Todas las áreas</button>
                <% areas.forEach(area => { %>
                    <button class="filter-button <%= selectedArea == area.materia_id ? 'active' : '' %>" 
                            onclick="window.location.href='/ahorcado_ranking?area=<%= area.materia_id %>'">
                        <%= area.nombre %>
                    </button>
                <% }) %>
            </div>

            <div class="stats-container">
                <div class="stat-card">
                    <h3>Total de Partidas</h3>
                    <p><%= data_ahorcado.length %></p>
                </div>
                <div class="stat-card">
                    <h3>Victorias</h3>
                    <p><%= data_ahorcado.filter(d => d.victoria).length %></p>
                </div>
                <div class="stat-card">
                    <h3>Mejor Tiempo</h3>
                    <p><%= Math.min(...data_ahorcado.map(d => d.tiempo)) %> seg</p>
                </div>
            </div>

            <% if (selectedArea) { %>
                <h2 class="area-title">Ranking de <%= areas.find(a => a.materia_id == selectedArea).nombre %></h2>
            <% } %>

            <table class="ranking-table">
                <thead>
                    <tr>
                        <th>Posición</th>
                        <th>Jugador</th>
                        <th>Área</th>
                        <th>Aciertos</th>
                        <th>Fallos</th>
                        <th>Tiempo</th>
                        <th>Palabra</th>
                        <th>Resultado</th>
                        <th>Fecha</th>
                    </tr>
                </thead>
                <tbody>
                    <% data_ahorcado.forEach((usuario, index) => { %>
                        <tr class="ranking-row <%= usuario.victoria ? 'victoria' : 'derrota' %>">
                            <td>
                                <% if (index === 0) { %>
                                    <span class="medal">🥇</span>
                                <% } else if (index === 1) { %>
                                    <span class="medal">🥈</span>
                                <% } else if (index === 2) { %>
                                    <span class="medal">🥉</span>
                                <% } else { %>
                                    <%= index + 1 %>
                                <% } %>
                            </td>
                            <td><%= usuario.nombre_usuario %></td>
                            <td><%= areas.find(a => a.materia_id == usuario.id_area).nombre %></td>
                            <td><%= usuario.aciertos %></td>
                            <td><%= usuario.intentos_fallidos %></td>
                            <td><%= usuario.tiempo %> seg</td>
                            <td><%= usuario.palabra_jugada %></td>
                            <td><%= usuario.victoria ? '✅ Victoria' : '❌ Derrota' %></td>
                            <td><%= new Date(usuario.fecha).toLocaleDateString() %></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>

            <div style="text-align: center; margin-top: 2rem;">
                <a href="/ahorcado_intro" class="back-button">← Volver al Juego</a>
            </div>
        </div>
    </main>
</body>

</html> 